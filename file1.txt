#include <iostream>

using namespace std;

int main()
{
   int n;
   cout << " Enter the number of elements = ";
   cin >> n;
   int energy[n];
   for (int i; i<n ;i++){
    cin >> energy[i];
   }
  cout << "Enter the trageted value = ";
  int x; // x is the the target
  cin >> x;

    for (int i; i<=n-1;i++){
        if (energy[i]== x){
            cout << i;
            break;
        }
        else if (i==n-1 && energy[i] != x)
            cout << "-1";

    }

    return 0;
}
